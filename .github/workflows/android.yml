name: Build Sytbay Android v0.3
on:
  push:
    branches: [ android-app-build ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Build APK and AAB
        run: gradle :app:assembleDebug :app:assembleRelease :app:bundleRelease --stacktrace
      - name: Package exact source
        run: zip -r /tmp/SytbayAndroid-v0.3.0-source.zip app build.gradle.kts settings.gradle.kts gradle.properties README-ANDROID.md .github/workflows/android.yml
      - uses: actions/upload-artifact@v4
        with:
          name: sytbay-android-v0.3-debug
          path: app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        with:
          name: sytbay-android-v0.3-release-unsigned
          path: app/build/outputs/apk/release/app-release-unsigned.apk
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        with:
          name: sytbay-android-v0.3-aab-unsigned
          path: app/build/outputs/bundle/release/app-release.aab
          if-no-files-found: error
      - uses: actions/upload-artifact@v4
        with:
          name: sytbay-android-v0.3-source
          path: /tmp/SytbayAndroid-v0.3.0-source.zip
          if-no-files-found: error
